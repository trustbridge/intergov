Processors are simple scripts which run indefinitely and process events
from some queue-like sources. Exceptions are logged but shouln't stop the
script from executing the use case for next objects/messages.
